name: C/C++ CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: change directory
      run: cd build/
    - name: build
      run: cmake ..
    - name: make
      run: make
